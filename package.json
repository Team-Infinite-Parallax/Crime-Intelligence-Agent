{
  "name": "crime-intelligence-agent",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "zcatalyst-sdk-node": "^3.4.0"
  }
}
